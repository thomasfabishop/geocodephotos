# geocodephotos
reverse geocodes photos - from GPS coordinates to place names or address.

renames photos based on date and GPS coordinates - to YYYYMMDD format and address.

a simple version that cannot handle duplicates and a more complex one that does.
